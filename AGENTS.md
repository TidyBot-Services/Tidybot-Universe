# 项目回答规则

本文件适用于从 Tidybot-Universe 仓库启动、且工作目录位于本仓库内的 Codex 工作。独立的 Service 仓库不自动继承本文件。

回答本项目的现状、架构、完成度、缺口或“下一步”之前：

1. 先确认当前仓库、分支和未提交改动；不要把其他 checkout 或远端状态当作当前代码。
2. 检查与问题直接相关的实现、接口、测试和协议文件。历史对话与计划只能作为线索，不能作为当前事实的证据；不要求每次通读整个仓库。
3. 明确区分：代码已存在、自动测试通过、真实服务跑通、正式实验验收完成。不得把前一层说成后一层。
4. 对关键结论给出文件路径或实际测试结果；没有查到的内容标为“未核实”，不要猜测。
5. 涉及多个独立仓库或 Service 时，分别核查版本与调用关系，不默认它们已经同步。
6. 若用户只要简短回答，仍须先核查，再简要说明结论与依据。
